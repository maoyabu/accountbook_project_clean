<% layout('layouts/boilerplate') %>

<div class="row g-3">
  <div class="col-12">
    <div class="card">
      <div class="card-header text-white d-flex justify-content-between align-items-center" style="background-color: #3a7ca5;">
        <span>All About me レポート</span>
        <a class="btn btn-outline-light btn-sm" href="/allaboutme/report/mylist">レポート詳細</a>
      </div>
      <div class="card-body">
        <div class="fw-bold mb-2"><%= reportYear %>年<%= reportMonth %>月のMyリスト</div>
        <div class="row g-3">
          <div class="col-12 col-lg-6">
            <div class="fw-bold mb-2">先月新規追加（<%= reportNewItems.length %>件）</div>
            <% if (reportNewItems.length > 0) { %>
              <ul class="list-group small">
                <% reportNewItems.forEach(item => { %>
                  <li class="list-group-item">
                    <strong><%= item.title %></strong> &lt;<%= item.item %>&gt;
                    <small class="text-muted">（<%= new Date(item.entry_date).toLocaleDateString() %>）</small>
                  </li>
                <% }) %>
              </ul>
            <% } else { %>
              <div class="text-muted">新規追加はありません。</div>
            <% } %>
          </div>
          <div class="col-12 col-lg-6">
            <div class="fw-bold mb-2">先月完了/破棄（<%= reportCompletedItems.length %>件）</div>
            <% if (reportCompletedItems.length > 0) { %>
              <ul class="list-group small">
                <% reportCompletedItems.forEach(item => { %>
                  <li class="list-group-item">
                    <strong><%= item.title %></strong> &lt;<%= item.item %>&gt;
                    <span class="badge bg-secondary"><%= item.status %></span>
                    <small class="text-muted">（<%= new Date(item.update_date).toLocaleDateString() %>）</small>
                  </li>
                <% }) %>
              </ul>
            <% } else { %>
              <div class="text-muted">完了/破棄はありません。</div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12 col-lg-6">
    <div class="card">
      <div class="card-header text-white" style="background-color: #3a7ca5;">
        マイリスト
      </div>
      <div class="card-body">
        <% if (wantolistItems.length > 0) { %>
          <ul class="list-group small">
            <% wantolistItems.forEach(item => { %>
              <li class="list-group-item d-flex justify-content-between">
                <span><%= item.title %></span>
                <span class="text-muted"><%= item.item %></span>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <div class="text-muted">マイリストの登録がありません。</div>
        <% } %>
        <div class="mt-3">
          <a class="btn btn-outline-primary btn-sm" href="/allaboutme/wantolist">マイリスト</a>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12 col-lg-6">
    <div class="card">
      <div class="card-header text-white d-flex justify-content-between align-items-center" style="background-color: #3a7ca5;">
        <span>マイ日記</span>
        <a class="btn btn-outline-light btn-sm" href="/allaboutme/eventcal_events">My日記設定</a>
      </div>
      <div class="card-body">
        <% if (diaryEntries.length > 0) { %>
          <ul class="list-group small">
            <% diaryEntries.forEach(entry => { %>
              <li class="list-group-item d-flex justify-content-between align-items-center">
                <span class="text-truncate" style="max-width: 70%;">
                  <strong><%= entry.event || 'イベント未入力' %></strong>
                  <span class="text-muted ms-2"><%= entry.date ? entry.date.toISOString().split('T')[0] : '' %></span>
                  <span class="ms-2"><%= entry.content || '内容未入力' %></span>
                </span>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <div class="text-muted">マイ日記の登録がありません。</div>
        <% } %>
        <div class="mt-3 d-flex flex-wrap gap-2">
          <a class="btn btn-outline-primary btn-sm" href="/allaboutme/eventcal">今日のマイ日記</a>
          <a class="btn btn-outline-primary btn-sm" href="/allaboutme/export-diary">My日記書出し</a>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card">
      <div class="card-header text-white d-flex justify-content-between align-items-center" style="background-color: #3a7ca5;">
        <span>My History</span>
        <a class="btn btn-outline-light btn-sm" href="/history/categories">My History設定</a>
      </div>
      <div class="card-body">
        <% if (recentHistories.length > 0) { %>
          <ul class="list-group small">
            <% recentHistories.forEach(history => { %>
              <li class="list-group-item d-flex justify-content-between">
                <span>
                  <strong><%= history.category?.name || '未分類' %></strong>
                  <%= history.content ? ` / ${history.content}` : '' %>
                </span>
                <small class="text-muted"><%= history.entry_date ? new Date(history.entry_date).toLocaleDateString() : '' %></small>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <div class="text-muted">My Historyの登録がありません。</div>
        <% } %>
        <div class="mt-3 d-flex flex-wrap gap-2">
          <a class="btn btn-outline-primary btn-sm" href="/history/list">My History</a>
          <a class="btn btn-outline-primary btn-sm" href="/resume/myresume">My 履歴書</a>
        </div>
      </div>
    </div>
  </div>

  <div class="col-12">
    <div class="card">
      <div class="card-header text-white" style="background-color: #3a7ca5;">
        Myselfメニュー
      </div>
      <div class="card-body d-flex flex-wrap gap-2">
        <a class="btn btn-outline-primary btn-sm" href="/relation">My Relation</a>
      </div>
    </div>
  </div>
</div>
